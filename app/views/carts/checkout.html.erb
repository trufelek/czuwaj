<% if !current_user.address.nil?%>
	<%= current_user.address%>
<% end %>
<% if !current_user.city.nil?%>
	<%= current_user.city%>
<% end %>


<p>Wartość zamówienia: <%= Order.find_by(iscart: 0, user_id: current_user.id).subtotal%> zł

<p><%= link_to "Potwierdź", order_items_finish_path%>